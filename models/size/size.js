class Size{

    /**
     * 
     * @param {Integer} id 
     * @param {Integer} itemID 
     * @param {string} size
     */
    constructor(id, itemID, size){
        this.id = id,
        this.itemID = itemID,
        this.size = size
    }
}

module.exports = Size;