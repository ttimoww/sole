class Size{

    /**
     * 
     * @param {Integer} id 
     * @param {Integer} itemID 
     * @param {Array} sizes 
     */
    constructor(id, itemID, sizes){
        this.id = id,
        this.itemID = itemID,
        this.sizes = sizes
    }
}

module.exports = Size;